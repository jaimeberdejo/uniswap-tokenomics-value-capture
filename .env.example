# Copia este archivo a .env y rellena tus claves. NUNCA commitees .env.
# Solo se necesitan para RE-extraer datos (scripts/export_dune.py).
# El notebook corre desde data/*.csv sin ninguna clave.
DUNE_API_KEY=
COINGECKO_API_KEY=
ETHERSCAN_API_KEY=
